# Plan: Liquidation of Bot System

## Scope

Remove the entire Lua-based bot system: BotManager, BotEventPublisher, BotHandler, Lua runtime, DB tables, frontend page, all event wiring, and the gopher-lua dependency.

## Files to DELETE (7 files)

| File | Lines |
|------|-------|
| `apps/backend-go/internal/bots/runtime.go` | 571 |
| `apps/backend-go/internal/bots/lua_api.go` | 752 |
| `apps/backend-go/internal/bots/lua_api_extended.go` | 350 |
| `apps/backend-go/internal/bots/runtime_test.go` | — |
| `apps/backend-go/internal/bots/README.md` | — |
| `apps/backend-go/internal/api/handlers/bot_handler.go` | 542 |
| `apps/backend-go/internal/api/handlers/bot_handler_test.go` | — |
| `apps/backend-go/internal/api/handlers/bot_events.go` | 126 |
| `apps/backend-go/internal/api/handlers/bot_events_test.go` | — |
| `apps/web/src/pages/Bots.tsx` | 807 |

## Files to EDIT (11 files)

### 1. `apps/backend-go/cmd/server/main.go`
- Remove `import "github.com/gomo6/backend/internal/bots"` (line ~10)
- Remove BotManager creation + Start + defer Stop (lines 111–117)
- Remove `botManager` param from `routes.SetupRoutes(...)` call (line 126)

### 2. `apps/backend-go/internal/api/routes/routes.go`
- Remove `botManager interface{}` param from `SetupRoutes` signature (line 22)
- Remove `botEventPublisher` init (line 58)
- Remove `rpcHandler.SetBotEventPublisher(...)` (line 90)
- Remove `universalHandler.SetBotEventPublisher(...)` (line 93)
- Remove `botHandler` creation + `SetBotManager` (lines 100–101)
- Remove entire bot routes block (lines 133–146)

### 3. `apps/backend-go/internal/models/models.go`
- Remove Bot, BotLog, BotStats, CreateBotRequest, UpdateBotRequest structs (lines 345–394)

### 4. `apps/backend-go/internal/models/models_test.go`
- Remove TestBot, TestBotLog, TestBotStats, TestCreateBotRequest, TestUpdateBotRequest, TestUpdateBotRequest_PartialUpdate tests (lines ~594–730)

### 5. `apps/backend-go/internal/api/handlers/universal.go`
- Remove `botEventPublisher *BotEventPublisher` field (line 26)
- Remove `SetBotEventPublisher` method (lines 39–42)

### 6. `apps/backend-go/internal/api/handlers/universal_crud.go`
- Remove `h.botEventPublisher` nil check and calls (lines 446, 465–467, 479–481)

### 7. `apps/backend-go/internal/api/handlers/rpc.go`
- Remove `botEventPublisher *BotEventPublisher` field (line 38)
- Remove `SetBotEventPublisher` method (lines 65–66)
- Remove botEventPublisher checks and calls (lines 326–327, 519–520)

### 8. `apps/backend-go/internal/api/handlers/likes.go`
- Remove all 4 `// Publish bot event` blocks that publish to `"bot:events"` Redis channel (lines ~123–134, ~181–191, ~278–289, ~336–346)

### 9. `apps/web/src/App.tsx`
- Remove `const Bots = lazy(() => import("./pages/Bots"));` (line 42)
- Remove `<Route path="bots" ...>` (line 157)

### 10. `apps/web/src/pages/Settings.tsx`
- Remove the entire "Боты" card block (lines ~491–503)

### 11. `apps/web/src/api.d.ts`
- Remove all bot-related type definitions (lines ~2970–3230 approximately — `/api/v1/bots`, `/api/v1/bots/{id}`, `/api/v1/bots/{id}/toggle`, `/api/v1/bots/{id}/logs`, `/api/v1/bots/{id}/stats`, and `Bot`, `BotLog`, `BotStats`, `CreateBotRequest`, `UpdateBotRequest` schemas)

## Dependency cleanup

### 12. `apps/backend-go/go.mod`
- Remove `github.com/yuin/gopher-lua v1.1.1`

### 13. `apps/backend-go/go.sum`
- Run `go mod tidy` to clean up

### 14. Vendor cleanup
- Run `go mod vendor` to remove `vendor/github.com/yuin/gopher-lua/` directory

## Database migration

### 15. New migration: `migrations/047_drop_bot_system.sql`
```sql
-- Drop bot system tables (from migrations 017, 033)
DROP TRIGGER IF EXISTS trigger_limit_bot_logs ON bot_logs;
DROP TRIGGER IF EXISTS trigger_update_bot_updated_at ON bots;
DROP FUNCTION IF EXISTS cleanup_old_bot_logs();
DROP FUNCTION IF EXISTS limit_bot_logs_per_bot();
DROP FUNCTION IF EXISTS update_bot_updated_at();
DROP TABLE IF EXISTS bot_stats;
DROP TABLE IF EXISTS bot_logs;
DROP TABLE IF EXISTS bots;
```

**Do NOT delete old migration files (017, 018, 021, 022, 033, 034)** — they already ran. The new migration cleanly drops what they created.

## Pre-flight verification (confirmed safe)

- **Redis `bot:events` subscriber**: ONLY in `runtime.go` (being deleted). No other worker/listener uses this channel.
- **Frontend `/bots` links**: Only in `Settings.tsx` (in plan to edit). No sidebar, mobile menu, or other nav has it.
- **Handler tests**: No test files reference `SetBotEventPublisher` or `botEventPublisher`. No test fixes needed.

## Execution order

1. Create the DB migration file
2. Edit all backend Go files (remove bot code)
3. Run `go mod tidy && go mod vendor` to clean dependency
4. Edit frontend files (remove Bots page, route, settings link, API types)
5. Delete all bot files
6. Verify: `go build ./...`, `go vet ./...`, `go test ./...`
7. Verify frontend: `npx tsc --noEmit -p apps/web/tsconfig.app.json`
8. Verify lint: `golangci-lint run --timeout=5m ./...`
9. Run full CI: `./scripts/ci-local.sh quick`

## Verification checklist

- [ ] `cd apps/backend-go && go build ./...` — compiles
- [ ] `cd apps/backend-go && go vet ./...` — no issues
- [ ] `cd apps/backend-go && go test ./...` — tests pass
- [ ] `cd apps/backend-go && go mod tidy` — clean
- [ ] `npx tsc --noEmit -p apps/web/tsconfig.app.json` — no TS errors
- [ ] `npm run lint --workspace=@gomo6/web` — no lint errors
- [ ] No remaining references to `BotManager`, `BotEventPublisher`, `bot_handler`, `gopher-lua`, `"bot:events"` in codebase
