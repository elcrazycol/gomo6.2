# Оптимизация мобильного меню-бургера

## Задача
Обновить мобильное бургер-меню: убрать дублирующиеся элементы, удалить страницу досок, вынести дропсы из контейнера профиля.

## Изменения

### 1. MobileMenu.tsx — основная работа

**Убрать ID из панели профиля** (строка 168):
- Удалить `<div className="text-sm text-muted-foreground mt-1">ID: {user.id.slice(0, 8)} {accountNumber && `(${accountNumber})`}</div>`

**Вынести дропсы из панели профиля** в отдельную секцию:
- Убрать блок дропсов (строки 170-184) из панели профиля
- Добавить отдельную секцию "Дропсы" после панели профиля (перед или после Настроек):
```tsx
<button
  type="button"
  onClick={() => {
    setOpen(false);
    window.dispatchEvent(new CustomEvent('open-drops-shop'));
  }}
  className="block w-full"
>
  <Button variant="ghost" className="w-full justify-start ...">
    <Droplets className="w-4 h-4 mr-2" />
    Дропсы
    {dropsData && <span className="ml-auto text-sm text-muted-foreground">{dropsData.drops}</span>}
  </Button>
</button>
```

**Компактный навигационный ряд** (строки 193-241) — оставить только G-сабы:
- Удалить кнопку "Доски" (строки 196-206)
- Удалить кнопку "Поиск" (строки 218-228)
- Удалить кнопку "Настройки" из ряда (строки 229-239)
- Оставить только "G-сабы" и убрать `overflow-x-auto` если ряд не скроллится

**Импорты** — удалить `Grid3X3` и `Search` из lucide-react (больше не используются).

### 2. App.tsx — удалить маршрут /boards

- Строка 17: удалить `const BoardsView = lazy(...)`
- Строка 141: удалить `<Route path="boards" element={...} />`

### 3. Index.tsx — убрать кнопку досок

- Строки 485-493: удалить кнопку "Основные доски"

### 4. IndexRouter.tsx — убрать секцию досок

- Строки 311-339: удалить секцию "Доски" (заголовок + список)
- Строка 374: удалить "Случайные доски"

### 5. BoardsView.tsx — удалить файл

Файл больше не нужен.

## Файлы для изменения

| Файл | Действие |
|------|----------|
| `apps/web/src/components/MobileMenu.tsx` | Редактировать |
| `apps/web/src/App.tsx` | Удалить route + lazy import |
| `apps/web/src/pages/Index.tsx` | Удалить кнопку досок |
| `apps/web/src/pages/IndexRouter.tsx` | Удалить секцию досок |
| `apps/web/src/pages/BoardsView.tsx` | Удалить файл |

## Верификация

1. `npx tsc --noEmit -p apps/web/tsconfig.app.json` — типы
2. `npm run lint --workspace=@gomo6/web` — линтер
3. Визуально: бургер-меню на мобилке — проверить что нет ID, дропсы отдельной строкой, нет дублирующего поиска, одна кнопка настроек, нет досок
4. `/boards` — должна вернуть 404
