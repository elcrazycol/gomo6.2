# hadolint configuration
# Ignore DL3018 (pin versions in apk add) — build-stage deps don't need exact pins,
# and the VPS blocks outbound HTTPS so version-qualified fetches fail.
ignored:
  - DL3018
